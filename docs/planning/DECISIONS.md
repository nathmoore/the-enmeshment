# Decisions & Decision Log

> The project's **permanent decision log** — the record of what was decided and why,
> so future sessions don't re-litigate settled calls. Newest first.
> Running scratch / notes appendix at the bottom.

## Decision log

| Date | Decision | Rationale | Revisit? |
|---|---|---|---|
| 2026-07-01 | **Second-playtest pass (v0.6): breadth-over-depth (range is the experience) + restore the level-one "would you stand up" fantasy.** The second self-playtest honed onto one archetype fast (3 of 4 questions on the self-vs-others axis) and felt flat, and the between-question "assessment" register repeated every turn ("got tired fast"); separately, the maintainer's recurring itch — the *Tomorrow, When the War Began* "am I the person who'd stand up and fight?" fantasy — surfaced as the missing **level-one** fun (the v0.5 pass built level two rich but level one thin). **(1) Breadth before depth.** New `elicitation-playbook.txt` §1 **rule 4**: spread exchanges across DIFFERENT dimensions, never two in a row on one axis, **even when confident** — a confident read spends spare exchanges going WIDE, not re-drilling the winner. Breadth is both the quiz pleasure AND what makes the verdict feel *earned*; drilling one axis then classifying on it is **leading the witness** (flat, pre-decided). `instructions.md` CLASSIFICATION: "flesh it out, don't gather more" → **"WIDEN not deepen"**; §2 backbone reinforced (the three anchors are three different axes = the breadth guarantee; don't skip [P] on an early thesis). **(2) Level-one fight fantasy.** New §3 **STANDING UP / CONVICTION** dimension — everyday, behaviourally-specific ("last time you saw something that wasn't right — say something or let it go?"), never "would you be brave"; reads conviction / agency under pressure across the set. New §5 **STAKES BRIDGE** (connect an everyday answer up to "who you'd be if it came to it") + an IDENTITY undertone line. **Guard held:** implied stakes, deadpan, ordinary courage — no doom, no collapse, no real-world, no survivalism (GAME-DESIGN pillar 4). Reconciles with the "threat dropped from the store headline" call (2026-06-13), which explicitly kept threat as in-fiction seasoning. **(3) Register variety.** §5 rations REFLECTION + CONFIRMATION (a couple per session, not every turn — the wry mirror-then-question tic is what "got tired fast") + a VARY THE CONNECTIVE REGISTER rule; the NUMERIC-ANCHOR (1-10) probe is flagged to play EARLY for a faint "measured / assessment" texture (maintainer wanted the quant vibe kept). Synced: `elicitation-playbook.txt` §1–§5, `instructions.md` (budget **v0.6 = 7,977 / 8,000**), GAME-DESIGN.md (pillar 1 now names both layers; §4 breadth principle). | Fast single-axis honing makes even a *correct* read feel tautological ("asked me social things, called me social") — breadth is what converts a correct guess into "how did it *know*", and it's the BuzzFeed-style pleasure (range + "is that me?") players show up for. Restoring level one isn't contrary to the no-doom tone: the fight fantasy is *character under pressure / ordinary courage* — human texture (pillar 4), not survivalism — and it **marries** the centaur theme (standing up = refusing to be a reverse-centaur). Confidence should govern *when to file*, never *how varied the questions are*; fusing them (the old "flesh it out, don't gather more") is exactly what produced the narrowing. | Validate in the next playtest: does the interview now probe a *range* (and not tire), does the courage dimension land without tipping into doom, does the 1-10 early beat read as "measured" not clinical, and does the level-one undertone set up the verdict's resistance-role payoff? |
| 2026-07-01 | **Playtest-driven interview + verdict pass (v0.5): prime the human/AI-boundary theme early via the *centaur / reverse-centaur* axis; concrete door-then-room across two turns; sanction quiz-texture doors + a new FORCED-CHOICE BRACKET; tighten the in-chat verdict to a real teaser.** Triggered by the first self-playtest (flat / over-validating, double-barrelled questions, a verdict longer than the linked page with no tease, and an opening that never invoked the AI-relationship the store copy promises). **(1) Theme priming.** New research doc [HUMAN-ROLES-RESEARCH.md](../HUMAN-ROLES-RESEARCH.md) grounds the level-two theme as *which faculties we defend as human* (deep thinking, taste, judgment, decision, care, meaning, coordination) and, above all, the **centaur / reverse-centaur** framing — not *human or machine* but *which way the arrangement runs*. Concord primes it early and reads it as an invisible layer; **routing is unchanged** (still Want/Need/Lie — the centaur read is priming + dossier-sting flavour, never a router). **(2) Concrete boundary doors.** `elicitation-playbook.txt` §3 gains a CONCRETE BOUNDARY DOORS pool (message-to-a-friend, what-to-watch, taste-in-books, a-number-you-didn't-work-out) — tangible, multiple-choice-textured on-ramps replacing the too-abstract open seeds; each tailors to *volunteered* texture only (never a forbidden fact). **(3) Two-turn split.** The door (closed opener) and the draw-out (where signal + technique live) are now SEPARATE turns, never stacked into one message (fixes the double-barrelled read). **(4) Doors reversed.** §1's flat "never a lettered a/b/c/d menu" ban → guarded concrete doors: binary / 1-10 / short-concrete allowed, the open room always follows, quiz-texture is spice not default. **(5) New technique.** FORCED-CHOICE BRACKET (§5) — our adaptation of the parent BRACKETING: a loaded menu whose *broken frame* ("neither — I'd do it myself") is the richest read; both live, pick by moment; plus a sanctioned single-scale NUMERIC-ANCHOR PROBE. **(6) AI-mention rule revised.** "Only the politeness meme is about AI" → **everyday-framed, AI only as a mundane choice** (refusal-line / arrangement read), never a literacy probe; the politeness meme stays the one *social*-relationship question. **(7) Verdict tightened.** Extends the 2026-06-13 thin-verdict call: the in-chat extract is hard-capped to a short teaser (2-3 sentences + one specific tell) + a one-line tease, **no invented metrics/columns**, depth reserved for the linked dossier. Synced: `elicitation-playbook.txt` §1–§5, `instructions.md` (THE GAME / CLASSIFICATION / OUTPUT FORMAT / KNOWLEDGE FILES; budget **v0.5 = 7,897 / 8,000**), new `HUMAN-ROLES-RESEARCH.md`, CLAUDE.md file-map. **Considered and dropped:** a stakes-menace *cold open* (didn't earn its length; priming via boundary doors does the job without over-promising doom the verdict softens). | The playtest exposed that the interview never made the player *feel analysed* (warm mirroring crowded out the analytic moves) nor *primed for the level-two AI question* (everyday-before-tech banished all AI to the last beat), and the verdict cannibalised its own click (spent the full read in chat). The centaur axis fixes all three: a non-doom, non-partisan framing that (a) lets everyday questions read as boundary probes, (b) makes the analysis visible via concrete doors + bracketing, and (c) matches the store promise. Reversing the lettered-menu ban is safe because what that rule protected — no literacy-measuring 4-option catalogues; the open room always carrying the signal — survives as explicit guards, while the quiz texture *is* the "being analysed" fun the playtest missed. FORCED-CHOICE BRACKET is on-ethos, not manipulation: the player who breaks the frame is asserting the very faculty the game celebrates, and Concord rewards it. Priming the theme deliberately (vs survey-style broad→narrow anti-priming) is correct because provoking reflection on the human/AI relationship *is* the point (Bluey level-two), not contamination. | Validate in the next self-playtest: does the interview now feel analytic (bracketing / loaded-door land, not gimmicky), does concrete-door → draw-out lower cognitive load without losing signal, does the theme prime register without naming tech, and does the tightened verdict drive the click? Verify the HUMAN-ROLES-RESEARCH §II / §V.3 synthesis (a design construct; some URLs flagged) before any of it reaches a player surface |
| 2026-07-01 | **`archetype-links.txt` folded into `archetype-dossiers.txt` (retired as a standalone file); the in-chat verdict source is pinned to `archetype-dossiers.txt`.** Each dossier block now carries its own `LINK (emit verbatim at handoff): <url>` line, so the file the GPT retrieves to *voice* the verdict also carries the exact URL to *hand off* — one retrieval, both. Reverses the [CONTENT-ARCHITECTURE.md](CONTENT-ARCHITECTURE.md) §4 call to keep the URL map as "deliberately its own tiny file." Also resolves a latent source ambiguity: `instructions.md` OUTPUT FORMAT previously pulled the in-chat P1 from the "spice-knob (archetypes.txt)" (2026-06-13 thin-verdict entry) *while* `archetype-dossiers.txt` also claimed to be the in-chat source — now settled: **`archetype-dossiers.txt` = the in-chat verdict source (voiced P1 + one FIELD NOTE tell) + the handoff link; `archetypes.txt` = the routing engine (Want/Need/Lie), read during the interview, not at verdict.** `instructions.md` KNOWLEDGE FILES also now names the 11 canonical archetypes as a closed set (exact-match discipline so a misnamed classification can't silently drop the link). Synced: instructions.md, CLAUDE.md file-map, site/README.md, site/.eleventy.js. | Custom-GPT retrieval is best-effort per-turn and chunked (MECHANICS §I) — co-locating the voice + link in one block sharply cuts the "voiced a verdict but dropped/guessed the URL" failure, which a separate tiny file leaves exposed (both are needed at the *same* moment). The "single file to re-author on domain change" property §4 wanted survives intact: the 11 LINK lines share one base string → one find/replace. Naming the 11 in the always-loaded instructions closes the exact-match gap `archetype-links.txt` required but couldn't guarantee from a retrieved chunk. | Validate in Epic 2 self-test that verdicts reliably emit the correct URL from the retrieved dossier block; re-author the 11 LINK lines when `enmeshed.xyz` goes live |
| 2026-07-01 | **GPT "Description" + conversation starters locked → new [`src/gpt-config.md`](../../src/gpt-config.md); the description "threat-vs-resistance" framing (open since 2026-06-30) is resolved.** The single GPT **Description** field (one string, used both on the store card and atop the GPT page) is set to: *"What kind of problem would you be? In a near future where the state's intelligence has grown enmeshed with an AI's, Concord — its cheerful new screening agent — needs a quick read on you before your next placement. A personality quiz that profiles which way you'd lean if things ever came apart."* (295/300 chars). Supersedes the 2026-06-13 description *draft*. The **4 conversation starters** — `Hello` / `Let's begin` / `Hi, I'm here to get my OPPPA please?` / `Do I have to be here?` — are the true cold open (they set the deadpan tone before Concord speaks; the wary "do I have to be here?" is itself first free signal). All non-instruction Builder fields now live in `src/gpt-config.md` (name, store title, description, starters), versioned rather than typed-once-into-the-UI. No "how to respond" guidance was added to instructions.md — the cold open + starters make the natural mode self-evident. | The Description does double duty — curiosity-gap primer for a cold arrival *and* the "what this is" a player repeats to a friend — so it must read as a **personality quiz** (findability + shareability) while opening the AI-state gap. "Threat" was considered for the headline and dropped: it promises menace the dossiers deliberately *don't* pay off (they land warm/affectionate), so store→play→verdict stays one register; "threat" survives only as in-fiction seasoning Concord can tease. Store *title* stays keyword-locked on "resistance" (2026-06-13); the Description carries the hook. Starters + description belong in the repo because they carry more of the cold-start experience than any single instruction line. | Validate the curiosity-gap → open-rate and the starter mix in Epic 2 self-test; confirm the 300-char Description limit + starter wording render as expected in the Builder |
| 2026-07-01 | **Disclaimer amended (GUARDRAILS v1.0): the retention claim is dropped; a transparency line replaces it.** The ratified disclaimer's final sentence changes from *"Nothing here was recorded or retained."* to *"The whole game is open to inspect on GitHub."* Synced across all homes: GUARDRAILS §2 (authoritative), `instructions.md` OUTPUT FORMAT, and the OUTPUT-SPEC illustrative reference (now points at GUARDRAILS rather than duplicating). | "Nothing here was recorded or retained" **overclaimed** — the game keeps no record and runs no analytics (GUARDRAILS §4), but the *platform* (OpenAI/ChatGPT) processes and may retain the chat under its own terms, which we don't control. GUARDRAILS §4 already concedes exactly this, so the disclaimer contradicted our own posture. This is an **accuracy fix, not a loosening** — the actual data posture (§4: no memory, no analytics, no tracking) is unchanged; we simply stop asserting a platform-level guarantee we can't back. The replacement leans on the repo being **fully open (CC0)** — "don't take our word for it, read the whole thing" is a stronger, truthful reassurance and on-theme for a privacy-respecting profiler. The nuanced platform caveat lives where it belongs: the public "what this game does with your words" card (GUARDRAILS §7, copy = Epic 2). Per the user's steer, the disclaimer itself makes **no AI/data-handling claim** at all. | Author the §7 transparency card so the platform-handling truth is stated plainly somewhere player-facing; ensure the hosted dossier pages render "GitHub" as a working link to the repo |
| 2026-06-30 | **Cold open revised: a light self-intro + consent ask replaces "assume the frame, launch straight in"; the upfront name-flex is cut.** Concord now opens with a one-line self-introduction and a consent beat — *"Hi — I'm Concord. Before your re-matching I just need to run your OPPPA — Official Personality Predictive Profiling Assessment. A few quick questions — that alright?"* (OPPPA = the in-fiction assessment name; keep the term, flex the rest). On assent it rolls straight into the first question ("Lovely — let's start with…"). Two related cuts: (1) the original "assume the player already knows the frame — don't explain it" reveal rule (from the canon-first slice) is **narrowed** to "don't monologue the *world*," while a light *intro* is now explicitly wanted — players don't know the frame, so pacing the entry is the designer's job, not a deep-end drop; (2) the upfront name-flex line ("you don't even need to give me your name — I'd rather you didn't") is **removed** as out-of-character and attention-drawing. The consent reply is now also the first free behavioural read (eager / wary / joking). | The "deep-end / they-know-the-frame" instinct assumed a player who understands the conceit; real players won't, so an unintroduced interview reads as confusing, not bold. The consent beat is triple-duty: in-character bureaucracy, a real ethical nicety, and free signal before the backbone. The name-flex was self-defeating — *announcing* "I won't ask your name" is exactly the draw-attention move the deadpan avoids, and it risked shutting down a friendly "hi, I'm Nathan." The "it never even asked my name" brag lands harder as a *retrospective* realisation (GUARDRAILS §6 privacy flex) than as an upfront declaration, so cutting the line strengthens it. Store *title* stays keyword-locked on "resistance" (DECISIONS 2026-06-13); OPPPA lives in Concord's mouth, not the listing. | Marketplace *description* threat-vs-resistance framing still open (lean resistance for keyword + no-doom tone); validate the OPPPA opener + consent-as-signal in Epic 2 playtest |
| 2026-06-30 | **Follow-up cap relaxed: "one per exchange, maximum" → "usually one; up to two or three when the player is engaged and volunteering."** Revises the LOCKED ELICITATION-SPEC §4 stop-rule line (and the §2 exchange-unit definition, which read "+ ≤1 follow-up"). The limit is reframed as **pressure, not a turn count** — rapidly probing a reluctant player reads as interrogation, but following a willing talker for a second or third beat is just good conversation. Crucially, **extra follow-ups stay *inside* the one exchange** (same thread, deeper) and do **not** increment the 3–6 *exchange* count; if one exchange runs long on a chatty player, the later ones stay short so the ~5-exchange arc still lands. Carried into `instructions.md` THE GAME (door-then-room rule) and `elicitation-playbook.txt` §1. | The hard "one follow-up" cap under-served the richest signal the research names — the "doorknob comment" second tier ("the yes/no is the door, the next sentence is the room", MECHANICS §IV) — by cutting off a player who *wanted* to keep going. The real failure mode the original rule guarded against is **felt interrogation**, which is a function of *pressure on a reluctant player*, not raw turn count; re-targeting the limit at pressure keeps that guard while freeing genuine elaboration. Keeping extra beats inside the exchange preserves the 3–6 length model intact (the band counts exchanges, not questions). | Tune against playtest: does a 2–3-beat exchange ever read as interrogation despite engagement, and does the ~5 arc hold when early exchanges run long? |
| 2026-06-13 | **In-chat verdict trimmed to thin (P1 "they got me" + one-line tease); full P2 is page-only.** instructions.md `OUTPUT FORMAT` had expanded OUTPUT-SPEC §1's *"previews the rest"* into a fully voiced **P2** ("why the system is worried") beat — re-voicing the level-two read that now lives on the hosted dossier page + `archetype-dossiers.txt`. Realigned: the in-chat verdict voices the name + the **P1 spice-knob tell** + a **one-line tease**, then hands off the exact URL; the **footnote + verbatim disclaimer stay in-chat** (normative, both homes); **P2's level-two read is page-only**. | The hosted page is the rich artefact; re-voicing P2 in chat duplicated it (drift risk) without strengthening the viral hook (which is the P1 tell + the link unfurl). Not a spec change — §1 already said "previews the rest… rather than reciting it"; this makes instructions.md match, and pins down that "preview" = a one-line tease. Disclaimer stays in-chat (GUARDRAILS-normative). | Validate the thin verdict still lands "they got me" + drives the click in Epic 2 playtest |
| 2026-06-13 | **Archetype voice samples authored (×11) + extracted to their own knowledge file `archetype-dossiers.txt`.** Drafted the player-facing dossier prose for all 11 — per archetype: **P1** (a true, flattering, cold-legible personality read) · **ASSESSMENT** (the trajectory verdict: what they'd bring to a *hypothetical, not-yet-existing* AI resistance + **one** sting, teasing the predict-your-trajectory conceit) · **FIELD NOTES** (3 below-fold dossier bullets). Register locked through iteration: **third-person Directorate "file" voice** (not 2nd-person BuzzFeed), but it must read **stone-cold as a personality result first** — lore is *seasoning*, the surveillance sting lands **late and gentle**, and the set is built so the **sting varies in position/flavour** card-to-card (so it reads as a group-chat compare, not a formula). Canon-true to the same-day refinement: the system **never fools anyone** (open secret), the read is **anticipatory/indifferent/actuarial** ("legibility, not guilt"). **Structural call:** the voiced prose lives in its **own file**, *not* inline in archetypes.txt. **One engine, three homes:** character → `archetypes.txt`; routing → ELICITATION-SPEC §8; voice → `archetype-dossiers.txt`. | The output is the product (MECHANICS §I) and travels by screenshot, so each result must read cold as "they got me" with the lore as a curiosity hook, not homework — early drafts that led with the lore/sting, or over-stung the flattery, flattened the personality and the story. Flattering-with-**one**-sting + leading with the resistance-role serves both Bluey layers at once (the *Tomorrow-When* friend-group "who'd be useful" game × the human–AI reflection). Extracting to a dedicated knowledge file (vs the inline placement the plan first used) fixes a real maintainability/drift risk: archetypes.txt is a ~500-line character/design doc, so a distributed source-of-truth was fiddly to tune *as a set* and noisy for RAG. Register source is now **instructions.md `OUTPUT FORMAT`** (output-templates.txt retired same day). | Validate the cold-read + "they got me" + the varied stings in Epic 2 playtest / screenshot tests; author the 11 `site/files/<slug>.md` dossier pages **and** the in-chat verdict *from* this file (keep the homes in sync) |
| 2026-06-13 | **`output-templates.txt` finalised (Epic 2) — the voiced in-chat verdict template.** Authored the agent-facing container the GPT *generates* the in-chat dossier from (a fill-shape, never recited; per-archetype prose stays in archetypes.txt / voice samples authored separately). Two non-obvious calls beyond a literal read of OUTPUT-SPEC §2.1: **(1) the voiced verdict carries the footnote + disclaimer**, not just the above-fold core. §2.1 frames the in-chat verdict as "the above-the-fold core, voiced" and files the footnote *below* the fold on the hosted page — but the footnote is the second-order share hook and the disclaimer is normative-in-both-homes (§4/§5; GUARDRAILS §2/§5), so both ship in the in-chat template; the **field-notes substance** is the part the verdict *gestures at and links out to* rather than reciting (it's the reward for opening the hosted page). **(2) Disclaimer inlined verbatim**, not deferred — GUARDRAILS reached **v1.0** (the string is ratified, §2), so the template carries the exact words with a "never paraphrase, never cut" flag, superseding the stub's "pending GUARDRAILS v1.0." Also folded the under-18/decline off-ramp and the share-card into the same file (both per OUTPUT-SPEC §6/§7). | OUTPUT-SPEC §2.1's fold split is about *what the hosted page lays out*, not a claim that the in-chat verdict drops the footnote/disclaimer — the prompt's deliverable list and §4/§5's both-homes language resolve the apparent gap in favour of including them. Inlining the now-ratified disclaimer (vs pointing at GUARDRAILS) keeps the agent's single source for the verbatim string in the file it generates from, lowering the risk it paraphrases a string that must never vary. | Validate the voiced template + footnote against playtest in Epic 2 (does it read as a register-shift, fit the fold when voiced, land "they got me"); wire the exact dossier URLs once `archetype-links.txt` is authored |
| 2026-06-13 | **Canon-lore-first slice of Epic 2 authored + conflict-forward framing locked.** Ran the canon-first slice (the Epic 2 lead item): light-touch promotion to [CANON.md](../CANON.md) §1–3, full rewrite of `lore-codex.txt`, fixed `instructions.md` IDENTITY, and authored the public `/scenario/` page (`site/scenario.md`, renamed from `/lore/`). Four locks: **(1) Storytelling stance — conflict-first.** Story is driven by conflict; every beat is tested by *do you want to know what happens next?* So lead with the **reach** — by 2038 the state↔AI line is *already gone* (accomplished, by convenience) and the system has just grown its *first eyes* and is reaching to profile disposition/networks/coordination-capacity **ahead of any of it forming** — and treat "no resistance has formed yet" as the **backdrop that makes the reach eerie**, not the headline. CANON §3 now states the read is **anticipatory, not retrospective** ("legibility, not guilt"). **(2) Factions retired from the codex.** The stale Pockets list (Margin Walkers / Stacks / Long Table) is removed from `lore-codex.txt`, finishing the 2026-06-12 canon deletion + the no-faction-axis call; resistance stays *latent/unformed*, never a named organised group. **(3) Persona corrected.** `instructions.md` IDENTITY rewritten from the retired "defected profiler" to **Concord, intake agent in good standing** (per 2026-06-11), plus a reveal rule (assume-frame-known; launch into the interview; if pressed, answer minimally + redirect) and the player's job-automation premise held lightly. **(4) Scenario page = plain authorial statement.** `/scenario/` (titled "The scenario", renamed from `/lore/`) is an out-of-character, literal explanation of the posited 2038 (game-makes-sense-for-cold-readers); in-world/flourish pages can come later. Plus a durable **surface-budget + layering** note in `lore-codex.txt` header and [EPIC-2-IMPLEMENT.md](EPIC-2-IMPLEMENT.md) | The codex was *stale*, not just stubbed (it still listed cut factions), and the persona contradicted canon — both would have propagated into Epic 2 dossiers/templates. The conflict-forward reframe is the documented thesis made vivid: it's exactly [RESEARCH-SYNTHESIS](../RESEARCH-SYNTHESIS.md) findings #2 (anticipatory, coordination-focused — "the profile becomes the crime") and #3 ("keeping the kindling dry"), grounded in WORLD-RESEARCH §II/§V and the STORY-SANDBOX §2 lived-clock (resistance organises once the new order *sets*; this one hasn't). The "inert vs conflict" distinction is the storytelling guard for all future player-facing prose | Validate the framing in Epic 2 playtest (does the cold-open + dossier pull "what next?"); full instructions.md prose + the 11 dossier pages still to author |
| 2026-06-13 | **One GPT for v1; no faction second axis.** The game ships as a single GPT — the intake-screener experience. No separate lore-explorer GPT; no faction/cell second axis in v1. | Single-mode keeps the build tractable for MVP; the 11 archetypes cover the design space. A second axis (11 × N cells) adds complexity without a clear payoff at this stage; lore-explorer is a possible post-launch addition | Revisit post-launch if players want to explore the world separately or a faction layer feels like a clear upgrade |
| 2026-06-13 | **Output spec started → [OUTPUT-SPEC.md](OUTPUT-SPEC.md): the dossier-as-artefact design, parallel to ELICITATION-SPEC.** Owns the EPIC-1 "Output spec" deliverable's *thinking* (shape, not prose). Load-bearing locks: (1) **two output homes** — the *voiced in-chat verdict* (generated, `output-templates.txt`) vs the *standardised hosted page* (`site/files/<slug>.md`), kept separate so a miss is debuggable (mirrors the elicitation two-homes); (2) the dossier is **built to be screenshotted** — the **fold** (classification + summary fit one mobile screen; substance lives below it) and the **elevated-BuzzFeed × deadpan-bureaucratic register**, shareable-first; (3) a **gesture-don't-dissect tone guard** — the artefact is built *from* the TAS arc (Want/Need/Lie/Ghost) but **never wears the scaffolding** (no naming the "Lie", no therapy-speak), even though the arc is open in the repo; (4) the **"why the system is worried" beat** does double work — the level-one *Tomorrow-When* friend-group fantasy × the level-two human–AI question — drawing on the archetype's Peg/Edge **threat read as dossier story-texture** (the one place threat legitimately surfaces; still *not* a routing driver); (5) the **disclaimer** is a **fixed, normative, always-present** safety string (≠ the per-archetype footnote), final wording deferred to GUARDRAILS v1.0; (6) the **share-card** = name + one-liner + hook (≠ the dossier; no disclaimer on it); (7) a **decline / under-18 / lore-explorer off-ramp** (no profiling, never an interrogation). The **"they got me" mechanic** lives here; its **per-archetype spice-knob content stays in archetypes.txt** | The output is the product (MECHANICS §I): a shareable artefact has 4 jobs (distinctiveness / name-as-identity / the "they got me" line / footnote-as-share-hook) and must beat the Barnum failure with *specificity* (MECHANICS §IV.D). It travels by screenshot, so the core read must fit a phone screen and read in the share register, or it doesn't spread. The TAS edge is the *engine*, but worn aloud it's clinical and off-tone — so it informs the read without surfacing. Splitting artefact-design (this doc) from delivery/hosting-plumbing ([CONTENT-ARCHITECTURE.md](CONTENT-ARCHITECTURE.md)) and from the per-archetype content (archetypes.txt) keeps each miss localisable, same rationale as the elicitation two-homes (MECHANICS §IV.D-bis) | Final per-archetype dossier prose (×11), footnote questions (×11), voiced-template wording, share-card one-liners + images = **Epic 2**; disclaimer wording + decline/under-18 handling = **GUARDRAILS v1.0**; validate the fold/register/"they got me" against playtest + screenshot tests in Epic 2 |
| 2026-06-13 | **Name locked: game/brand = "The Enmeshment"; GPT store title = "The Enmeshment: AI Resistance Profile Quiz".** The evocative brand name is kept for lore, share-card, and result identity; the store *listing* title appends a keyword tail (`: AI Resistance Profile Quiz`) for discoverability. Colon (over em-dash) for the standard "Title: subtitle" convention. Store **description** drafted: *"What kind of resistance would an AI classify you as? A short interview from inside The Enmeshment — a near-future where government decisions have become entangled with AI. Answer honestly and receive your profile, your archetype, and your dossier. Part personality quiz, part speculative fiction. (No real data collected.)"* | Resolves the long-standing naming tension (scratch / MECHANICS §III): "The Enmeshment" is strong brand but a **zero-keyword** search term — undiscoverable alone until it has word-of-mouth equity. GPT Store discovery is **search + sharing**, not sidebar browsing: the name field is the indexed + link-preview asset, so it must front-load function keywords (*AI / resistance / profile / quiz*); sidebar/card truncation only affects *re-finding*, so a clipped tail is acceptable as long as the brand half survives it. "Resistance" is both the strongest keyword and the hook — kept in the title (mild premise tip judged worth the findability). Description packs secondary keywords + pre-empts the privacy worry (GUARDRAILS) with the closing parenthetical | Revisit if store search behaviour changes or the brand gains enough traction to drop the keyword tail (pure "The Enmeshment"). Title kept ≤50 chars for the name field | All code, prompts, lore, and site content dedicated to the public domain — no rights reserved, no attribution required. Replaces the provisional MIT-code / CC BY-SA-lore split. CC0 §4 carries an explicit as-is / no-liability disclaimer, so there is no need for a separate MIT warranty clause | The original split was a mechanical default (MIT = right vocab for code; CC = right vocab for prose) — not a strategic choice. CC0 covers both domains cleanly and the project's mission (spreading a conversation about the human-AI relationship) is better served by removing all friction. Attribution and share-alike were never positive goals. CC0 does not waive trademark, so the project name can still be protected separately if desired | — |
| 2026-06-13 | **"Classification logic" deliverable retired & folded into [ELICITATION-SPEC.md](ELICITATION-SPEC.md) §8 "Routing principles."** The rigid signal→archetype *mapping* framing is dropped (it predated the move off structured questions). Routing is now **probabilistic best-fit over free-form answers**, driven by a **character-first engine** — each archetype built as a formed character (**Want / Need / Lie** + **BuzzFeed reverse-engineering**, the [transformational-arc kit](../MECHANICS-RESEARCH.md) now grounded in MECHANICS **§IV.D-bis**). That one engine feeds **two layers in two homes**: **"who they are"** (the formed characters, uplifted as CHARACTER lines in [archetypes.txt](../../src/knowledge/archetypes.txt)) vs **"how to tell them apart"** (the routing mechanics — discriminant signatures, overlap/confused-pair discriminators, performer risk — relocated from archetypes.txt into ELICITATION-SPEC §8). Output = **one archetype label → one dossier**; **only the 11** are routes (not the Off-Gridder); **no separate containment classification**. **OCEAN/CANOE demoted** to a minor corroborating signal (never the primary discriminator); **session length is framing only**, not a routing signal. Routing threshold = the stop rule's "signal sufficient" (shared, §4/§8.6) | "Classification logic" implied a rigid forward-scored rubric, but the runtime is a stateless model re-reasoning over a transcript with **no confidence variable** (MECHANICS §I) — best-fit inference from a character lens fits the medium; trait-scores don't. The **two-home split is deliberately for playtest debuggability**: a wrong result localises to *either* "the character definition is off" (fix archetypes.txt) *or* "the character's right but the routing guidance misfired" (fix the spec) — fused, every miss would be ambiguous. OCEAN was an *input* in the research (MECHANICS §IV.C, since flagged/demoted), never the spine | Per-archetype character essence + the discriminant signatures/discriminators validated in Epic 2 playtest; Self-Optimiser body-vs-career split confirmed then |
| 2026-06-13 | **Interview length is driven by a story/experience floor, not the routing floor — modal ~5 exchanges (band 3–6), counted in *exchanges* not questions.** Two distinct minimums: the **routing floor** (~3 answers is enough to *classify* — clusters + Bayesian inference, MECHANICS §IV.C/§I) and the higher **experience floor** (~4–5 to feel *read* not *sorted*). The reveal is timed by the experience floor: the agent can usually classify earlier and **deliberately doesn't**, never handing off before ~75% of the session (figure borrowed as input from the non-canon Save-the-Cat beat-sheet). The counting **unit = the *exchange*** (one anchor probe + ≤1 follow-up), retiring the ambiguous "3–6 questions" framing; 3 and 6 are *diegetic tails* (fast = "instantly legible"; slow = "unusual pattern"), not the everyday range. Full Plan-level decisions in the new **[ELICITATION-SPEC.md](ELICITATION-SPEC.md)** (two floors; exchange unit + length; 3-anchor backbone; stop rule; technique palette; never-ask principle). Beat-sheet annotated with the exchange mapping (STORY-SANDBOX §7) | The "how many questions?" question was unanswerable because it conflated *classify* with *satisfy*. The fun/"they got me" beats must land **before** the reveal or an accurate result still feels cheap; being witnessed for a beat or two *is* the product (MECHANICS §I, §IV — flow channel = signal quality). Counting follow-ups as separate questions both inflated the felt interrogation and undersold the "doorknob comment" second-tier signal. ~5 modal is what makes 3/6 *mean* fast/slow | Modal 5 vs 4 to be tuned against drop-off in Epic 2 playtest; figures are guidance, not quotas |
| 2026-06-13 | **Public site = Eleventy → GitHub Pages (Actions source); dossier pages at `/files/<slug>/`.** The link-out targets (and landing / lore / reading-room) are built with **Eleventy** in a dedicated [`site/`](../../site/) dir and deployed to **GitHub Pages via the "GitHub Actions" source** (not the legacy root/`/docs` mode, which would publish internal design `docs/`). One dossier **layout + 11 markdown data files** → frozen `/files/<slug>/` URLs (the 11 slugs are listed in [CONTENT-ARCHITECTURE.md](CONTENT-ARCHITECTURE.md) §4). Per-page OG/Twitter meta; no third-party trackers. Scaffold + stubs created 2026-06-13; deploy workflow is **manual-only** until Pages is enabled. Internal `docs/` stays the design layer, `site/` is the curated public face | Repo-as-source-of-truth (site lives beside canon, free, no server); DRY (one template, not 11 hand-built pages); the dossier **is the product**, so it needs design control + per-archetype **share-unfurl cards** — the virality engine (MECHANICS §I) — which rules out docs-tools (Read the Docs / MkDocs *look* like technical docs) and a single homepage (can't carry per-archetype previews). Netlify/Cloudflare Pages also viable but split hosting from the repo home; not worth leaving GitHub at this scale. "Actions source" avoids exposing internal `docs/` as the website | **URL base set 2026-06-13:** `nathmoore/the-enmeshment` project page (`pathPrefix: "/the-enmeshment/"`); custom domain `enmeshed.xyz` is the planned upgrade (flip prefix to `/`; GitHub auto-redirects old links). Revisit host only if build/preview needs outgrow Pages |
| 2026-06-13 | **Dossier-delivery model locked + knowledge stays uploaded (not live-linked).** The verdict is delivered as a **paraphrased, in-voice classification** (agent voices it from `output-templates.txt`) **+ the archetype's exact dossier-page URL**, copied from a new `archetype-links.txt` lookup map (the agent must **never construct/guess** a slug). Knowledge files remain **uploaded `.txt`** (re-uploaded on release), **not** linked to live GitHub. Un-parks the per-archetype GitHub-Pages link-out idea (scratch); building the pages is a future workstream (Epic 2/3) that does **not** block Epic 1. Captured in [CONTENT-ARCHITECTURE.md](CONTENT-ARCHITECTURE.md) (the now-started Epic 1 content-architecture deliverable) | GPT-runtime research ([MECHANICS §I "Platform capabilities & constraints"](../MECHANICS-RESEARCH.md), June 2026): (1) the runtime keeps **no confidence variable** — adaptive flow is emergent, so anchor questions + a written stop rule are load-bearing, not optional; (2) **verbatim-from-knowledge is unreliable** (paraphrase-preference + copyright filters) and a recited file plays worse than a voiced verdict → generate within a template; (3) emitting a link needs **no browsing/Action**, and is reliable only if the URL is **copied from a map**, not guessed; (4) knowledge **cannot** point at a live file — it's uploaded + RAG-indexed (20 files / 512 MB / 2 M tokens; chunked; ~110k-token truncation), and for a small slow-changing canon uploaded `.txt` beats the latency/flakiness of browse/Action live-fetch | Build the Pages dossier site + author `archetype-links.txt` in Epic 2/3; revisit live-fetch only if canon grows large or fast-changing |
| 2026-06-12 | **Canon spine promoted; LORE.md → [CANON.md](../CANON.md); 2038 is canon.** The authoritative layer is renamed **CANON.md** and reframed as a *truth-set* — canon = what's *true*, not what's *shown* (reveal is per-surface; not all canon reaches the player or even Concord). Promoted from STORY-SANDBOX: the **2038 setting**; Concord-as-intake-pilot on the re-allocation stream (reads cold, never asks your name, stays warm throughout, abrupt forward-to-next-desk hand-off); the actuarial verdict ("capable of X, predicted not to" — archetype is what's delivered, trajectory/containment the internal lens); "no one's home" as the **working assumption** (never resolved into someone in charge). The stale faction layer (Margin Walkers / Stacks / Long Table) and the defected-profiler frame are **deleted** (recoverable in git). CLAUDE.md updated: the "no dates" rule narrows to "set in 2038; other surfaces date-light + no real-world actors unless logged here" | The intake-agent reframe (06-11) and archetype lock (06-12) left LORE.md §4–5 stale; the sandbox had held since the weave. Naming the year was breaking the evergreen *player-facing* rule, but as *canon* (writers' truth) it sets the cusp cleanly and the evergreen rule was always about player surfaces — the rename makes that split explicit. Factions aren't load-bearing in the single-mode intake game | Reveal policy for 2038 per-surface in Epic 2; archetype-link-to-GitHub-pages idea **decided 2026-06-13** (see top row + CONTENT-ARCHITECTURE.md) |
| 2026-06-12 | **Epic 1 (Plan) lightened: it locks DECISIONS + architecture, not authored artifacts.** The elicitation *playbook*, the LORE *prose*, and the instructions.md *prose* are all **Epic 2 (Implement)** knowledge-file/authoring artifacts — the research (MECHANICS §IV etc.) already did the design thinking. Plan keeps only the gating decisions (anchor questions, confidence-threshold stop rule, signal->archetype mapping, never-ask boundaries) and a single **content-architecture** task (instructions.md section skeleton + the instructions-vs-knowledge split, against the 20-file limit + 8k/1,800 ceiling). The standalone "section-by-section budget allocation" deliverable is **dropped** — folded into content architecture; precise char-counting happens while writing in Epic 2 | Mirrors the lore split (research -> plan-decisions -> implement-artifact); avoids false precision (budgeting before prose exists) and over-planning authoring tasks; keeps Plan to load-bearing locks | — |
| 2026-06-12 | **Lore/scenario works in three layers (avoids the "bible vs canon" word-clash).** (1) **STORY-SANDBOX.md** (formerly STORY-TEST.md) — a deliberately-*specific*, internally-woven sandbox for testing the whole scenario as one picture (timeline, AI trajectory, the intake agent, phases, Nobody/Someone invariant, privacy flex). Explicitly **NON-CANON**: may drift, may be revised, not necessarily the truth used downstream. Epic 1 gating artifact. (2) **LORE.md** — the **authoritative canon**, distilled from the brainstorming once it holds; the single reference downstream work cites for what it can/can't use. (3) **lore-codex.txt** — LORE.md condensed into the GPT knowledge file | A thin "spine" hides incoherences and yields worse prose; the AI builds a better world when forced to commit to specifics we can inspect whole. But that specific weave must be clearly fenced off as non-authoritative so tentative details don't leak into canon by accident — hence a separate sandbox doc, with LORE.md as the single authoritative source. Reframes the intake-agent change (which superseded the defected-profiler lore) as a coherence task to resolve before name/output/voice | Brainstorming is tentative; only LORE.md is authoritative |
| 2026-06-12 | **Archetype set LOCKED at 11 (pending playtest).** Cut Reformer + Machine Agitator; folded Climber → Self-Optimiser; restored Social Linchpin (rare Priority-Interest tail, balancing the rare No-Concern Model Citizen). Names settled — notably **Skeptic** (over Cynic: name flatters, dossier deflates) and **Wildcard** (over Chaos Agent: "Agent" implied villain-intent). Full set, selection criteria, set-level criteria, and per-type rationale now in [`src/knowledge/archetypes.txt`](../../src/knowledge/archetypes.txt) — the **source of truth**, ahead of MECHANICS §II / RESEARCH-SYNTHESIS | Rebalances away from an over-crowded "favourable/Join" end toward the resister/illegible roles that power the level-one "who in our group is the X" fantasy; covers all 5 intelligence objectives; every name passes the two-voices test | **Routability** of adjacent pairs (Model Citizen/Power User; Self-Optimiser body-vs-career; Organiser/Linchpin) to be pressure-tested in Epic 2; Off-Gridder/Survivalist held on a watchlist |
| 2026-06-12 | **Paste-prompt / "Field Kit" mode cut — single-mode game (adaptive interview only).** Removes the two-mode design (Mode A: run a self-profiling prompt in your own AI tool, paste the result back; Mode B: in-game interview). The game is now *only* the oblique in-game interview. Deletes `src/prompts/profile-prompt.md`; drops the Field Kit Epic 1 deliverable and the "combine Mode A+B" question | The paste-prompt was the one mechanic that actively invited players to *extract* data from other AI tools and paste it back — exactly the oversharing risk that is the game's single biggest real-world hazard (MECHANICS §III, risk-2). Cutting it strengthens the privacy-as-feature pillar (the profiler that never even asks), removes a guardrail surface we'd otherwise have to police (GUARDRAILS §3), simplifies the build, and is truer to the intake-agent reframe | Settled; the privacy "flex" is now cleaner |
| 2026-06-12 | **Interview model = adaptive elicitation, not a fixed question script.** The intake agent improvises ~3–6 oblique, fun questions using its own judgment, *diverging* on responses, rather than walking a pre-authored Q1–Q10 branch. Reframes the Epic 1 "question bank" into an **elicitation playbook** (question *types* + interview techniques + the signal each reveals) + a few reliable **anchor questions** (incl. the politeness meme) + the confidence-threshold stop rule | Follows from the intake-agent reframe (below) and the finding that free-form, persona-driven inference beats form-filling (MECHANICS §I, §F); keeps the game fun, replayable, genuinely conversational. **Two consequences raise *existing* risks:** (1) guardrails must now bound questions generated on the fly — "what it must never ask" becomes load-bearing and the oversharing risk (MECHANICS §III, risk-2) grows; (2) classification consistency must be protected via anchor questions + signal→archetype rules so divergent paths still converge. Needs a small **interview/elicitation-technique research top-up** — KUBARK (WORLD §III) is the in-world-ironic precedent; add cognitive / indirect / projective interviewing | Protect through Epic 1; revisit if playtest shows inconsistent classification or guardrail drift |
| 2026-06-11 | **In-game frame changed** from "defected profiler who shows you your file" to a **charming intake / screening agent in good standing** — friendly, concise, reads you *cold* (no private data), and classifies you as a **predicted trajectory** ("which way you'd go, and whether you will"), not a resistance badge. Supersedes [CANON.md](../CANON.md) §4 (then LORE.md §4) | Better grounded (TSA SPOT / IJOP / KUBARK *are* intake-screening, WORLD §II–III); sharper interview→dossier reveal (the mask slips as it files you); truer to the indifference thesis than a sentimental "fond" profiler; dissolves the strained "every archetype is resistance, incl. Conventional User" rule. In-fiction guardrail reason shifts from "it defected" to "screening is narrow-scope by design" — privacy becomes a flex ("it never even asked your name") | Protect through Epic 1; LORE §4 + knowledge stubs to be rewritten |
| 2026-06-11 | Research split into three docs **by objective**: [WORLD-RESEARCH](../WORLD-RESEARCH.md) (world/scenario grounding), [MECHANICS-RESEARCH](../MECHANICS-RESEARCH.md) (how the game is built), [STORY-RESEARCH](../STORY-RESEARCH.md) (narrative/genre) | A single RESEARCH.md conflated distinct objectives; profiling/surveillance is a standout, reviewer-facing thread that deserves its own home | If a 4th research objective emerges |
| 2026-06-11 | **Scenario positioning locked**: drift / boiling-frog, triangulated — *reject* the precipice camp's threshold event (Aschenbrenner, AI 2027); *accept* the normal-tech camp's gradual diffusion (Narayanan/Kapoor) but *reject* its reassurance | We never have to speculate about an AI "moment." "Normal technology, abnormal outcome" is the original position | — |
| 2026-06-11 | **Control-horizon invariant**: the Enmeshment sits between "rule by Nobody" (gradual disempowerment) and "rule by a hidden Someone" (power concentration), and stops short of the singleton's closure. The Nobody/Someone ambiguity is **permanent** | Credible (both horizons are real literature), apolitical (no named villain), dramatically alive (unanswerable, not merely hidden). Revealing a cabal collapses the story into the rejected malevolent-AI lineage | Protect through Epic 1+ |
| 2026-06-11 | **Resistance target = state overreach** (state + AI enmeshed), *not* technology (Luddite correction). Resistance = illegibility = preserving the social substrate for a possible tipping point ("keeping the kindling dry") | Keeps the game out of technophobia and inside "what relationship do we want?"; gives "persist illegibly" a real stake | — |
| 2026-06-11 | The GPT's **character persona is the inference mechanism**, not decoration — a bland helpful-assistant voice produces measurably worse profiling *and* worse UX | Research finding (conversational personality inference); affects how strictly the Directorate persona is held in instructions.md | — |
| 2026-06-11 | GPT Builder hard limits **verified**: 8,000-char instructions; 20 knowledge files (≤512 MB each) | Unblocks Epic 1 instructions.md budgeting against a confirmed ceiling, not an assumption; CLAUDE.md prime directive #3 holds | Re-verify if OpenAI changes limits |
| 2026-06-11 | Research stats **fact-checked** in a fresh validation conversation (11 load-bearing claims): 9/11 confirmed. Huawei "Safe City" footprint corrected to ~73 deployments/52 countries (CSIS) vs marketing's 700/100; minor reframes to sluggish-schizophrenia "38%" and the metadata-inference wording. Corrections in WORLD-RESEARCH; full log in [FACT-CHECK-QUEUE](FACT-CHECK-QUEUE.md) | Closes the credibility-calibration loop; stats now safe to draw lore from | If new stats added |
| 2026-06-11 | **Meme calibration**: keep the "be polite to AI" meme's *marketing fun* and *credibility claim* on separate ledgers. Level-two grounding is the **unexamined ambiguity** of our politeness (habit/hedge/joke/unease), NOT a claim that people fear AI judgment | Habit reading is backed by Reeves & Nass CASA / *Media Equation* research (automatic, denied social reflex); the "insurance" survey numbers are commissioned PR research (Talker) that themselves cite "better output"/"habit", not fear. Overclaiming would undercut credibility | — |
| 2026-06-11 | Repo scaffolded; Research→Plan→Implement→Playtest, one epic per step | Playtest added: GPT instruction-tuning is inherently iterative | — |
| 2026-06-11 | Guardrails are normative and get ≥1,800 chars of instructions budget | Privacy-as-feature is core to the Bluey deep layer | — |
| 2026-06-11 | Single root CLAUDE.md, no sub-files | Repo too small to justify; revisit if src/ grows | If repo grows |

## Provisional — NOT yet decided (Epic 1 to lock)

Research *positions/seeds*, deliberately held open until Epic 1:

- ~~Archetype set~~ — **LOCKED 2026-06-12 (see decision log + [archetypes.txt](../../src/knowledge/archetypes.txt)).** 11 predicted-trajectory classifications under the intake-screener frame: the verdict is *which way you'd go + whether you will*, the sting lives in the *likelihood* ("capable of X, predicted not to"), with a deliberate output distribution — rare "No Concern" (Model Citizen) and rare "Priority Interest" (Social Linchpin) tails, a fat nuanced middle. STILL OPEN: the profiling **axes** that drive classification (how many the quiz actually probes — research suggests 5, realistically 3–4).
- Number of classification axes the quiz actually probes (research suggests 5; quiz realistically supports 3–4).
- ~~One GPT or two? Faction second axis in v1?~~ **DECIDED 2026-06-13:** one GPT, no second axis (see decision log).
- ~~Working title "The Enmeshment" — confirm or replace.~~ **LOCKED 2026-06-13** (see decision log): brand stays "The Enmeshment"; store title = "The Enmeshment: AI Resistance Profile Quiz".

## Scratch

- ~~Name candidates: The Enmeshment / Please & Thank You / Politeness Telemetry / The Unfiled~~ → **resolved** (decision log 2026-06-13).
- ~~**Naming tension (MECHANICS §III):** "The Enmeshment" is great brand, poor search keyword. Likely answer = two-part store title: evocative brand + keyword tail.~~ → **resolved 2026-06-13:** store title = "The Enmeshment: AI Resistance Profile Quiz" (brand stays for lore + share-card + result identity).
- Share-card idea: dossier renders nicely in monospace for screenshots
- Pages idea: "Reading Room" page built from the research bibliographies (WORLD-RESEARCH is the strongest reviewer-facing thread)
- The "you never know" Altman line is the tightest articulation of the whole premise — candidate epigraph.
- ~~**Parked (Epic 2+):** have the result link out to GitHub Pages / repo doc pages for each archetype~~ — **DECIDED 2026-06-13** (see decision log top row + [CONTENT-ARCHITECTURE.md](CONTENT-ARCHITECTURE.md)): in-voice verdict **+ exact dossier-page link** (copied from `archetype-links.txt`). The standardised page (the abrupt "file Concord forwards") is the hosted artefact; the in-chat moment stays voiced. Building the Pages site is the remaining Epic 2/3 execution.
